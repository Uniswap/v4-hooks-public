---
title: "StableFeeConfiguration"
description: "Holds the per-pool stable fee config + validation"
---

# StableFeeConfiguration

[Git Source](https://github.com/Uniswap/v4-hooks-internal/blob/d2a38d0852d124855be2202d8846e0259e3cfe28/src/stable/base/StableFeeConfiguration.sol)

**Inherits:** [HookRoles](/src/base/abstract.HookRoles), BlockNumberish, [IStableFeeConfiguration](/src/stable/interfaces/interface.IStableFeeConfiguration)

**Title:** StableFeeConfiguration

Holds the per-pool stable fee config + validation

## Constants

### MAX_OPTIMAL_FEE_E6

The maximum optimal fee in 1e6 precision: 1% (1e4 out of 1e6)

```solidity
uint256 public constant MAX_OPTIMAL_FEE_E6 = 1e4;
```

### MAX_TARGET_MULTIPLIER

The maximum target multiplier (100 = 100%, full closeBoundaryFee subtraction)

```solidity
uint256 public constant MAX_TARGET_MULTIPLIER = 100;
```

### STABLE_FEE_CONFIGURATION_STORAGE_LOCATION

<i>

keccak256(abi.encode(uint256(keccak256("uniswap.storage.StableFeeConfiguration")) - 1)) & ~bytes32(uint256(0xff))

</i>

```solidity
bytes32 private constant STABLE_FEE_CONFIGURATION_STORAGE_LOCATION =
        0x4e65cbff7fdec8e7b73e40370c540338cf2b602902c10f166d6a73c4a7ee9e00;
```

## Functions

<a id="_getstablefeeconfigurationstorage"></a>

### _getStableFeeConfigurationStorage

Accessor for this contract's ERC-7201 namespaced storage

```solidity
function _getStableFeeConfigurationStorage() internal pure returns (StableFeeConfigurationStorage storage $);
```

**Returns**

| Name | Type | Description |
| ---- | ---- | ----------- |
| $ | `StableFeeConfigurationStorage` |  |

<a id="updatefeeconfig-poolid-stablefeeconfig"></a>

### updateFeeConfig

Update the fee config for a pool

```solidity
function updateFeeConfig(PoolId poolId_, StableFeeConfig calldata feeConfig_)
        external
        onlyRole(CONFIG_MANAGER_ROLE);
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| poolId_ | `PoolId` | The ID of the pool |
| feeConfig_ | `StableFeeConfig` | The new fee config |

<a id="batchupdatefeeconfig-poolid-stablefeeconfig"></a>

### batchUpdateFeeConfig

Update the fee config for multiple pools in a single call

```solidity
function batchUpdateFeeConfig(PoolId[] calldata poolIds_, StableFeeConfig[] calldata feeConfigs_)
        external
        onlyRole(CONFIG_MANAGER_ROLE);
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| poolIds_ | `PoolId[]` | The IDs of the pools to update |
| feeConfigs_ | `StableFeeConfig[]` | The new fee configs for each pool, index-aligned with `poolIds` |

<a id="feeconfig-poolid"></a>

### feeConfig

The fee config for a pool

```solidity
function feeConfig(PoolId poolId)
        public
        view
        returns (uint24 k, uint24 optimalFeeE6, uint8 targetMultiplier, uint160 referenceSqrtPriceX96);
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| poolId | `PoolId` | The PoolId of the pool |

**Returns**

| Name | Type | Description |
| ---- | ---- | ----------- |
| k | `uint24` |  |
| optimalFeeE6 | `uint24` |  |
| targetMultiplier | `uint8` |  |
| referenceSqrtPriceX96 | `uint160` |  |

<a id="feestate-poolid"></a>

### feeState

The fee state for a pool

```solidity
function feeState(PoolId poolId)
        public
        view
        returns (uint40 decayingFeeE12, uint160 sqrtAmmPriceX96, uint40 blockNumber);
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| poolId | `PoolId` | The PoolId of the pool |

**Returns**

| Name | Type | Description |
| ---- | ---- | ----------- |
| decayingFeeE12 | `uint40` |  |
| sqrtAmmPriceX96 | `uint160` |  |
| blockNumber | `uint40` |  |

<a id="_updatefeeconfig-poolid-stablefeeconfig"></a>

### _updateFeeConfig

Internal helper to validate, reset state, and store fee config

```solidity
function _updateFeeConfig(PoolId _poolId, StableFeeConfig calldata _feeConfig) internal;
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| _poolId | `PoolId` | The pool ID to initialize |
| _feeConfig | `StableFeeConfig` | The fee config to validate and store |

<a id="_resetfeestate-poolid"></a>

### _resetFeeState

Internal helper to reset fee state

```solidity
function _resetFeeState(PoolId _poolId) internal;
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| _poolId | `PoolId` | The pool ID to reset fee state for |

<a id="_checkpoolinitialized-poolid"></a>

### _checkPoolInitialized

Reverts unless the pool already has a stored config

<i>

Config is only ever seeded by the hook's pool-initialization entrypoint, which enforces
the hook address and dynamic fee and initializes the pool on the PoolManager in the same
call. A nonzero reference price (validated nonzero on every write) therefore proves the
pool exists, uses this hook, and has a dynamic fee.

</i>

```solidity
function _checkPoolInitialized(PoolId _poolId) internal view;
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| _poolId | `PoolId` | The pool ID to check |

<a id="_validatek-uint256"></a>

### _validateK

Validate k (zero would mean instant decay)

```solidity
function _validateK(uint256 _k) internal pure;
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| _k | `uint256` | The k value to validate |

<a id="_validateoptimalfeee6-uint256"></a>

### _validateOptimalFeeE6

Validate the optimal fee

```solidity
function _validateOptimalFeeE6(uint256 _optimalFeeE6) internal pure;
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| _optimalFeeE6 | `uint256` | The optimal fee to validate |

<a id="_validatetargetmultiplier-uint256"></a>

### _validateTargetMultiplier

Validate the target multiplier (0-100, representing 0%-100%)

```solidity
function _validateTargetMultiplier(uint256 _targetMultiplier) internal pure;
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| _targetMultiplier | `uint256` | The target multiplier to validate |

<a id="_validatereferencesqrtpricex96-uint256"></a>

### _validateReferenceSqrtPriceX96

Validate the reference sqrt price

<i>

The optimal range is defined in terms of PRICE (not sqrt price):
[referencePrice * (1 - maxOptimalFee), referencePrice / (1 - maxOptimalFee)]
Since price = sqrtPrice², the sqrt price bounds are:
[referenceSqrtPrice * sqrt(1 - maxOptimalFee), referenceSqrtPrice / sqrt(1 - maxOptimalFee)]
Note: MIN_SQRT_PRICE is valid (inclusive) but MAX_SQRT_PRICE is invalid (exclusive) in v4.

</i>

```solidity
function _validateReferenceSqrtPriceX96(uint256 _referenceSqrtPriceX96) internal pure;
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| _referenceSqrtPriceX96 | `uint256` | The reference sqrt price to validate |

## Structs

### StableFeeConfigurationStorage

**Note:**

- **storage-location:** erc7201:uniswap.storage.StableFeeConfiguration

```solidity
struct StableFeeConfigurationStorage {
        /// @dev The fee config for each pool
        mapping(PoolId => StableFeeConfig) feeConfig;
        /// @dev The fee state for each pool
        mapping(PoolId => StableFeeState) feeState;
    }
```

**Properties**

| Name | Type | Description |
| ---- | ---- | ----------- |
| feeConfig | `mapping(PoolId => StableFeeConfig)` |  |
| feeState | `mapping(PoolId => StableFeeState)` |  |

